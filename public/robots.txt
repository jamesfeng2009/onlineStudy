# Robots.txt for lang-oria.com
#
# These paths are rendered as noindex via <Seo noindex />, so block
# them at the robots level too — saves Googlebot budget and prevents
# the "Crawled - currently not indexed" pile-up we saw on Search
# Console (phantom URLs like /de/languages/japanese/... etc).
#
# Sitemap advertises ONLY the canonical indexable URLs.

User-agent: *
Allow: /
Disallow: /api/
Disallow: /learn$
Disallow: /learn/
Sitemap: https://lang-oria.com/sitemap.xml

{
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/((?!assets/|favicon\\.svg).*)", "destination": "/" }
  ]
}

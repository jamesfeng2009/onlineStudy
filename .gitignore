# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnpm-store

# Trae
.trae

# Build output
dist
server/dist
server/**/*.js
server/**/*.js.map
server/**/*.d.ts
# Prisma generated client — build artifact, do not commit.
# Each environment regenerates it via `prisma generate`.
server/lib/prisma-generated/
dist-ssr
*.local

# Script caches (large dictionaries downloaded by build scripts)
scripts/cache
# Note: scripts/generated/quizzes and scripts/generated/listening
# are committed to the repo because they are source content used at
# build time, not throwaway caches.


# Env
.env
.env.local
.env.*.local

# Prisma generated client (optional)
# prisma/generated

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
